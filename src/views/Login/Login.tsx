import React from 'react';
import HeroLayout from '../../layouts/HeroLayout';

interface Props {
  [key: string]: any;
}

const Login = () => {
  return (
    <HeroLayout>
      <p>This is Login.</p>
    </HeroLayout>
  );
};

export default Login;
